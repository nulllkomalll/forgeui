export function Footer() {
  return (
    <footer className="border-t border-border px-4 py-6 text-sm text-muted-foreground">
      <div className="mx-auto flex max-w-6xl flex-col items-start justify-between gap-2 sm:flex-row sm:items-center">
        <p>ForgeUI — a component library documentation project.</p>
        <p>Built with React, TypeScript, and Tailwind CSS.</p>
      </div>
    </footer>
  );
}
